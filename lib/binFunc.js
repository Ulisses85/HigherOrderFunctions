function add (a, b) {
return a + b;
};

function sub (a, b) {
return a - b;
};

function mul (a, b) {
return a * b;
};
module.exports = {add, sub, mul};
/* Write three binary functions, *add*, *sub* and *mul* that
take two numbers and return their sum, difference and product.
`add(3, 4) // 7`
`sub(3, 4) // -1`
`mul(3, 4) // 12`
*/
